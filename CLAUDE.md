# CLAUDE.md

**The handbook for this repository is [`AGENTS.md`](AGENTS.md). Read that.**

It is deliberately not duplicated here. This project is worked on from more than one assistant —
Codex looks for `AGENTS.md`, Claude Code looks for `CLAUDE.md`, Cursor reads either — and two
copies of a handbook diverge within a fortnight. A stale handbook is worse than no handbook,
because it is believed.

For a plain-English overview of what the project is and where it stands, see
[`README.md`](README.md).
